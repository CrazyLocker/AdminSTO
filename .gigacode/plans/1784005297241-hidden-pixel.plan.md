# План рефакторинга: Удаление справочников и перенос вкладок в главное окно

## Текущее состояние

### Структура Dictionary
- **DictionaryView.java** - содержит 3 вкладки:
  - **Услуги** (createServicesPanel()) - управление услугами
  - **Запчасти** (createSparePartsPanel()) - управление запчастями
  - **Склад** (createStockPanel()) - управление складом

- **DictionaryController.java** - контроллер для всех трех вкладок
- Таблицы инициализируются статическими методами в DictionaryView

### Текущее использование в App.java
В главном окне текущие вкладки уже создают панели из DictionaryView:
```java
servicesTab.setContent(DictionaryView.createServicesPanel());
sparePartsTab.setContent(DictionaryView.createSparePartsPanel());
stockTab.setContent(DictionaryView.createStockPanel());
```

## Требуемое изменение

### Архитектура (утверждено пользователем)
- **Полностью автономные панели** с собственными контроллерами
- **DictionaryView и DictionaryController полностью удаляются**
- **Сохраняем статические геттеры для TableStateManager**

### Новые классы (6 штук)

#### View-классы (3)
1. **ServicePanel.java** - панель управления услугами (уже существует)
2. **SparePartPanel.java** - панель управления запчастями
3. **StockPanel.java** - панель управления складом

#### Controller-классы (3)
1. **ServicePanelController.java** - логика для услуг (уже существует)
2. **SparePartPanelController.java** - логика для запчастей
3. **StockPanelController.java** - логика для склада

### Панель-контроллер связь
Каждый Panel содержит Table и свой Controller:
- ServicePanel → ServicePanelController
- SparePartPanel → SparePartPanelController
- StockPanel → StockPanelController

Контроллеры обновляют DataStore и вызывают refresh у своих таблиц.

## План реализации

### Шаг 1: Создать SparePartPanel (НАЧАТЬ С ЭТОГО)
- Создать `src/main/java/com/autoservice/views/SparePartPanel.java`
- Перенести метод `createSparePartsPanel()` из DictionaryView (строки 191-315)
- Перенести метод `showAddSparePartDialog()` из DictionaryView (строки 376-490)
- Перенести метод `editSparePartDialog()` из DictionaryView (строки 493-619)
- Перенести метод `filterSpareParts()` из DictionaryView (строки 357-368)
- Создать статические геттеры для TableStateManager:
  - `public static TableView<SparePart> getTable()`
  - `public static TableView<SparePart> getSparePartsTable()`
- Создать `SparePartPanelController.java` с методами:
  - `addSparePart(SparePart part)`
  - `updateSparePart(SparePart part)`
  - `removeSpareParts(List<SparePart> parts)`
  - `refreshTable()`

### Шаг 2: Создать StockPanel
- Создать `src/main/java/com/autoservice/views/StockPanel.java`
- Перенести метод `createStockPanel()` из DictionaryView (строки 318-384)
- Перенести метод `editStockDialog()` из DictionaryView (строки 622-680)
- Создать статические геттеры для TableStateManager:
  - `public static TableView<SparePart> getTable()`
  - `public static TableView<SparePart> getStockTable()`
- Создать `StockPanelController.java` с методами:
  - `addStockIncome(SparePart part, double amount)`
  - `updateMinStock(SparePart part, double minStock)`
  - `refreshTable()`

### Шаг 3: Удалить DictionaryView и DictionaryController
- Удалить `src/main/java/com/autoservice/views/DictionaryView.java`
- Удалить `src/main/java/com/autoservice/controllers/DictionaryController.java`

### Шаг 4: Обновить App.java
- Изменить импорты:
  - Удалить `DictionaryView`
- Обновить создание вкладок:
  ```java
  servicesTab.setContent(ServicePanel.create());
  sparePartsTab.setContent(SparePartPanel.create());
  stockTab.setContent(StockPanel.create());
  ```
- Обновить TableStateManager с новыми именами таблиц:
  ```java
  TableStateManager.saveTableState(ServicePanel.getTable(), "servicesTable");
  TableStateManager.saveTableState(SparePartPanel.getTable(), "sparePartsTable");
  TableStateManager.saveTableState(StockPanel.getTable(), "stockTable");
  ```

### Шаг 5: Удалить тесты Dictionary (опционально)
- Удалить `src/test/java/com/autoservice/controllers/DictionaryControllerTest.java`
- Или обновить тесты для новых контроллеров

## Файлы для создания
1. `src/main/java/com/autoservice/views/SparePartPanel.java`
2. `src/main/java/com/autoservice/views/StockPanel.java`
3. `src/main/java/com/autoservice/controllers/SparePartPanelController.java`
4. `src/main/java/com/autoservice/controllers/StockPanelController.java`

## Файлы для удаления
1. `src/main/java/com/autoservice/views/DictionaryView.java`
2. `src/main/java/com/autoservice/controllers/DictionaryController.java`
3. `src/test/java/com/autoservice/controllers/DictionaryControllerTest.java` (опционально)

## Проверка
- Собрать проект: `mvn clean compile`
- Запустить приложение
- Проверить каждую вкладку (Услуги, Запчасти, Склад)
- Убедиться, что таблицы работают корректно
- Проверить сохранение состояния таблиц
- Проверить функциональность:
  - Добавление/редактирование/удаление услуг
  - Добавление/редактирование/удаление запчастей
  - Фильтрация запчастей
  - Внесение прихода на склад
  - Обновление минимального остатка

## Примечания
- ServicePanel и ServicePanelController уже существуют и могут служить шаблоном
- Код валидации и диалоговых окон переносится без изменений
- Все статические методы для TableStateManager должны быть сохранены
- DictionaryController - это легкий pass-through, логика в DataStore
